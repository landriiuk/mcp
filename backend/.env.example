# Legacy Next.js API (optional; app UI now uses Firebase Firestore).
# Keep secrets here only if you still run `npm run dev:backend`.

# APP_ENV=local
# APP_ENV=production
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres
#
# Never commit real DATABASE_URL / passwords. Use `.env` (gitignored).
