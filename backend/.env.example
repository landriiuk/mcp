# Local development (no Postgres required)
APP_ENV=local

# Production / hosted database — uncomment and set your connection string:
# APP_ENV=production
# DATABASE_URL=postgresql://user:password@host:5432/postgres
